import 'package:flutter/material.dart';



class NavigationDescriptionPage extends StatelessWidget {

  const NavigationDescriptionPage({Key key, int currentStop}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: <Widget>[
          
        ],
      ),
    );
  }
}
